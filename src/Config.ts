const U = 8;

export default {
    WIDTH: 8 * 10 * U,
    HEIGHT: 7 * 10 * U,
    CELL_SIZE: 6 * U,
    MOBILE_CELL_SIZE: 4 * U,
    PAD_VIEWPORT_HEIGHT: 20 * U,
    PLAYER_VELOCITY: 4 * U, // CELL_SIZE / 1
    UNIT: U,
    COUNTDOWN_BOMB: 2 * 1000,
};
